
const ListaTodoPage = () => {
// escribo la logica en javaScript aquí\
// ejemplo para obtener la lista de tareas desde una API, se puede usar fetch o axios. Recomiendo axios
  // const [todos, setTodos] = useState([]);

  // useEffect(() => {
  //   fetch("http://localhost:3000/api/todos")
  //     .then((res) => res.json())
  //     .then((data) => setTodos(data.todos));
  // }, []);

  return (
    <div>
      
    </div>
  )
}

export default ListaTodoPage
